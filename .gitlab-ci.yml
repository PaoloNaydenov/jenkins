stages:
  - build
  - deploy
before_script:
  - docker login -u $DOCKER_USER -p $DOCKER_PASS
build:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker build -t $CI_JOB_IMAGE .
    - docker push $CI_JOB_IMAGE
  tags:
    - test docker shell
  only:
    - main

deploy:
  stage: deploy
  image: docker:latest
  script:
    - docker pull "$CI_JOB_IMAGE"
    - docker run -d --name devops -p 5173:5173 "$CI_JOB_IMAGE"
    - "echo Job scs"
  tags: 
    - test docker shell
  only:
    - main
